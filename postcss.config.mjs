/**
 * PostCSS Configuration
 *
 * Tailwind CSS v4 is wired into Next.js via the `@tailwindcss/postcss` plugin.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
